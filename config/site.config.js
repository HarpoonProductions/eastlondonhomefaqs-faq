export const siteConfig = {
  name: "# → Site configuration (working ✅)",
  domain: "# → Sanity project creation (NEW)", 
  description: "# → Schema deployment (NEW)",
  theme: "orange",
  
  logo: {
    filename: "logo.png",
    width: 400,
    height: 120,
    alt: "# → Site configuration (working ✅)"
  },
  
  branding: {
    questionLabel: "Question",
    searchPlaceholder: "Search questions...",
    modalTitle: "Suggest a Question"
  },
  
  features: {
    search: true,
    suggestions: true,
    pwa: true,
    pushNotifications: false
  }
}

export default siteConfig
